---
description: 改 UI 时缩小改动面，避免顺带改业务/接口逻辑。
alwaysApply: true
---

# UI 改动边界

- **只改 UI 时**：优先只动对应组件与样式；除非为实现交互所必需，否则**不**改 `workspaceStore` / `canvasStore` 行为、`splitPane` / `closeTab` / openDocument 语义、以及后端 API。
- **确需改状态初始化**（例如避免假 DOM）：应写清原因，且**不**改分屏、关闭、打开文档等既有业务逻辑。
- **复合需求**（同时动前端状态与后端）时，在实现前区分：前端展示 / 状态 / 接口，再分别改。
